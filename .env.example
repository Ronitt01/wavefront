# Optional. Enables AI-phrased survival briefings via Gemini 2.5 Flash.
# Without it, Wavefront uses a deterministic fallback brief — still accurate;
# every number (arrival time, distance) is computed, never AI-generated.
GEMINI_API_KEY=
